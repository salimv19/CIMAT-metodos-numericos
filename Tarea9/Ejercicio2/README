README

main.cpp
AlgebraLineal.cpp
AlgebraLineal.h
IOMatrices.cpp
IOMatrices.h

El programa implementa la factorización QR de una matriz A.

El programa se compila con el comando:
g++ -std=c++0x main.cpp AlgebraLineal.cpp IOMatrices.cpp -o main

Creando un ejecutable de nombre "main"

El programa se ejecuta con el comando:
./main

Se pide al usuario el nombre del archivo que contiene la matriz A.

Se imprimen en consola las matrices Q y R, así como el valor de la norma 
de Frobenuis de (A - Q*R)
