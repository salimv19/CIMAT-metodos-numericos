README

main.cpp
AlgebraLineal.cpp
AlgebraLineal.h
IOMatrices.cpp
IOMatrices.h

El programa implementa el método de gradiente conjugado para encontrar la 
solución del sistema de ecuaciones Ax=b donde A es simétrica pos. def.

El programa se compila con el comando:
g++ -std=c++0x main.cpp AlgebraLineal.cpp IOMatrices.cpp -o main

Creando un ejecutable de nombre "main"

El programa se ejecuta con el comando:
./main

Se pide al usuario el nombre del archivo que contiene la matriz A, así 
como el archivo que contiene al vector b.

Se imprime en consola el número de iteraciones del método, así como el 
error y el vector solución o su norma.
